<!--
  ▶ JOB HUNTER — fill in this ONE file, then run. You only do two things:
      1) Pick your runtime + edit Preferences below.
      2) Paste your full résumé under the marker at the very bottom.
  Then run it the way that matches your runtime (see README.md → "Run it").

  PRIVACY: this is a guidance-only template shipped with the repo. Whatever you
  paste here stays on your machine unless you commit it. To make sure your edits
  are never committed, run once:
      git update-index --skip-worktree inbox/input.md
  Prefer a clean start? Copy samples/input.sample.md over this file.

  Track-only: nothing is ever auto-applied.
-->

# Job Hunter — Input

## Runtime

How do you want to run the agent? Set ONE value (`copilot` · `claude` · `codex` · `api`):

runtime: copilot

<!--
  copilot → VS Code / GitHub Copilot: run the `job-hunter` agent (or the prompts).
  claude  → Claude Code: open this repo and say "run the job hunt".
  codex   → Codex: open this repo and say "run the job hunt".
  api     → Headless, no agent: `python scripts/run.py` (needs OPENAI_API_KEY).
  Tip: `python scripts/run.py` reads this value and tells you exactly what to do.
-->

## Preferences

<!-- Fill in what you're targeting. Leave a field blank or "any" if it doesn't apply. -->

- **Target titles / role archetypes:**
- **Seniority:**
- **Locations:**
- **Work mode:** any
- **Salary floor:** (optional)
- **Industries of interest:**
- **Preferred / seed companies (optional):**
- **Blocked companies (never surface):**
- **Hard filters (avoid):**
- **Languages:**
- **Max companies (cap the hunt):** 30
- **Max roles per company:** 10

## Résumé

<!-- Plain text is best. Everything below the marker is treated as your résumé. -->

--- PASTE RESUME BELOW THIS LINE ---
